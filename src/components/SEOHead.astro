---
import { siteMeta, generateOrganizationLD, generateBreadcrumbLD, type SEOProps } from '@/lib/seo';

interface Props extends SEOProps {}

const {
  title,
  description,
  image = siteMeta.defaultImage,
  canonical,
  type = 'website',
  article,
  noindex = false,
  breadcrumbs,
} = Astro.props;

const resolvedTitle = title === siteMeta.name ? title : `${title} — ${siteMeta.name}`;
const canonicalUrl = canonical ?? Astro.url.href;
const ogImage = image.startsWith('http') ? image : `${siteMeta.url}${image}`;
const orgLD = generateOrganizationLD();
const breadcrumbLD = breadcrumbs ? generateBreadcrumbLD(breadcrumbs) : null;
---

<!-- Primary -->
<title>{resolvedTitle}</title>
<meta name="description" content={description} />
<meta name="robots" content={noindex ? 'noindex,nofollow' : 'index,follow'} />
<link rel="canonical" href={canonicalUrl} />

<!-- Open Graph -->
<meta property="og:type" content={type} />
<meta property="og:title" content={resolvedTitle} />
<meta property="og:description" content={description} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:image" content={ogImage} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:site_name" content={siteMeta.name} />
<meta property="og:locale" content={siteMeta.locale} />
{type === 'article' && article && (
  <>
    {article.publishedTime && <meta property="article:published_time" content={article.publishedTime} />}
    {article.modifiedTime && <meta property="article:modified_time" content={article.modifiedTime} />}
    {article.tags?.map((tag) => <meta property="article:tag" content={tag} />)}
  </>
)}

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:site" content={siteMeta.twitter} />
<meta name="twitter:title" content={resolvedTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />

<!-- JSON-LD -->
<script type="application/ld+json" set:html={JSON.stringify(orgLD)} />
{breadcrumbLD && (
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbLD)} />
)}
